# 产品范围

## 1. 目标用户

- 需要在消费级 GPU 上训练或微调开源 LLM 的算法工程师。
- 需要复现实验、比较模型和管理 Checkpoint 的研究人员。
- 需要展示分布式训练与 LLMOps 能力的个人开发者。
- 不具备 A100/H100 集群，但拥有多张 RTX 3090/V100 的实验室。
- 需要在本地开源模型上构建、评测和部署工具调用 Agent 的平台工程师。

## 2. 核心用户故事

### 训练工程师

> 我希望提交一个模型、数据和资源配置，系统自动检查硬件、推荐并行策略，并生成可恢复的训练任务。

### 研究人员

> 我希望从一个 Run ID 还原数据、代码、配置、环境和模型结果，以便验证实验是否可复现。

### 模型负责人

> 我希望候选模型只有在通过能力和性能门禁后才能进入部署阶段。

### 推理工程师

> 我希望将晋级后的模型一条命令部署，并获得统一的延迟、吞吐和显存报告。

### Agent 工程师

> 我希望使用稳定的 OpenAI 与 MCP 接口构建 DevOps 诊断 Agent，并用可复现的任务集衡量工具
> 选择、参数、恢复、证据引用和审批安全。

## 3. 产品边界

TinyLLM-System 管理的是单机多 GPU LLM 生命周期。

MVP 不覆盖：

- 多节点训练。
- 大规模调度集群。
- Kubernetes 多租户平台。
- 商业计费。
- 百亿级从零预训练。
- 自研 CUDA 算子。
- 完整 RLHF 平台。

## 4. 成功标准

- 能在共享的 10×RTX 3090 主机上稳定完成可复现的 1/2/4 卡实验，并如实记录资源边界。
- 能训练单卡放不下的模型。
- 能自动恢复分布式训练。
- 能自动生成基线与候选模型对比。
- 能阻止回退模型晋级。
- 能部署并压测生产候选模型。
- 能根据 Run ID 还原实验上下文。
- 能通过受控 MCP 工具完成有证据引用的 DevOps 诊断，并阻止未审批写操作。
- 能用 BFCL Core 与自建 DevOps Agent Suite 比较 Base、Candidate 和 Production。

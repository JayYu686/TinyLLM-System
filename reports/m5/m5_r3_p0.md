# M5.2-R3-P0 Teacher Pilot 实验报告

## 1. 当前结论

R3-P0 的配置、任务生成器、污染检查、Teacher 运行入口、严格 Trace 选择器、CPU 合成契约
Smoke 和失败路径已经完成。当前状态为 `IMPLEMENTED_AWAITING_REAL_TEACHER_PILOT`。

尚未产生真实 Qwen3-8B Teacher 结果，因此本报告不填写接受率、推理长度、显存或运行时间。
这些字段只会在真实 GPU 实验完成后从机器结果更新。

## 2. 冻结实验

| 项目 | 固定值 |
| -- | -- |
| 输入任务 | Config 20 + Log Diagnosis 20 |
| 每类语言 | 英文 14 + 中文 6 |
| Teacher | `Qwen/Qwen3-8B@b968826d9c46dd6066d109eabc6255188de91218` |
| 模式 | 原生 Thinking、BF16、GQA、单卡 |
| 采样 | Temperature 0.6、Top-p 0.95、Top-k 20 |
| 候选数 | 每题最多 2 个 |
| 生成上限 | 384 New Tokens |
| Trace 上限 | 192 Token |
| 序列上限 | 1024 Token |

每类必须接受至少 14 条，其中英文至少 10 条、中文至少 4 条。接受 Trace 还必须满足答案
精确匹配、自然闭合、Repeated 8-gram 不超过 5%、无重复行、全局 Trace 唯一和完整序列不
超过 1024 Token。

## 3. 已完成的前置验证

固定任务集共 40 条，任务集 SHA256 为：

```text
cf0fee91d2a3362cb3fd6567485b12596d278bac889fcfc8abc8453acc337f2f
```

与 200 条冻结 Reasoning Dev 和 100 条历史 Pilot 的 Exact Prompt、规范化 Prompt 及
Template Family 污染计数均为 0。CPU 合成契约 Smoke 验证了 40 条任务可通过完整构建流程；
该 Smoke 明确标记为 `model_generated=false`、`quality_metric=false`、`gpu_used=false`。

失败路径已经覆盖：

- Teacher 未闭合 Thinking；
- Final JSON 答案错误；
- 生成触及长度上限；
- 推理超过 192 Token；
- 重复规范化 Trace；
- 重复非空行；
- 完整序列超过 1024 Token；
- Dev 或历史 Pilot 污染。

## 4. GPU 实验与后续门禁

运行器在启动前要求干净 Git 工作树，校验所有冻结输入 SHA256，并执行忙卡、利用率和温度
Preflight。Raw Output、逐候选审计和逐条 Verifier 只写入私有 Artifact；公开 JSON 只保存
聚合计数、哈希、版本、硬件、时长和显存。

真实实验通过时，下一步冻结并实现每类 120 条、合计 240 条的正式任务扩展，再确定性选择
160 条 Trace。真实实验未通过时，保留拒绝原因并重新审查任务模板或 Teacher 控制策略，
不降低 Trace 标准、不增加生成上限。

## 5. 证据索引

- 冻结配置：[m5_r3_p0.yaml](../../configs/data/m5_r3_p0.yaml)
- CPU 合成契约 Smoke：[m5_r3_p0_cpu_smoke.json](raw/m5_r3_p0_cpu_smoke.json)
- P0 设计：[m5_r3_targeted_repair_design.md](../../docs/m5_r3_targeted_repair_design.md)
- 来源审计：[m5_r3_source_audit.json](raw/m5_r3_source_audit.json)

{
  "additionalProperties": false,
  "description": "Privacy-safe identity of one DistributedSampler partition.",
  "properties": {
    "rank": {
      "minimum": 0,
      "title": "Rank",
      "type": "integer"
    },
    "sample_count": {
      "exclusiveMinimum": 0,
      "title": "Sample Count",
      "type": "integer"
    },
    "sample_ids_sha256": {
      "pattern": "^[a-f0-9]{64}$",
      "title": "Sample Ids Sha256",
      "type": "string"
    },
    "schema_version": {
      "const": "1.0",
      "default": "1.0",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "rank",
    "sample_count",
    "sample_ids_sha256"
  ],
  "title": "DDPPartitionEvidence",
  "type": "object"
}

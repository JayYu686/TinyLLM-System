# Synthetic data fixtures

The JSON fixtures in this directory were authored for TinyLLM-System tests. They are synthetic,
contain no upstream dataset records, and are dedicated to the public domain under CC0-1.0.

Field names mirror the pinned upstream schemas so import behavior can be tested without network
access or redistribution of source data.
